# Database Configuration
DB_MASTER_URL=jdbc:postgresql://localhost:5432/app_db
DB_MASTER_USER=user
DB_MASTER_PASSWORD=password

# Optional Replica Configuration (Defaults to Master if omitted)
# DB_REPLICA_URL=jdbc:postgresql://localhost:5432/app_db_replica

# Redis Configuration
REDIS_MASTER_HOST=localhost
REDIS_MASTER_PORT=6379

# Optional Redis Replica Configuration (Defaults to Master if omitted)
# REDIS_REPLICA_HOST=localhost
# REDIS_REPLICA_PORT=6379
